const models = {
    userModel: require('./users'),
    transactionModel: require('./transaction'),
    liabilityModel: require('./liability'),
    categoryModel: require('./category'),
    assetsModel: require('./assets'),
    accountsModel: require('./accounts'),
};

module.exports = models;